This module adds the possibility to select a different warehouses in each sale line.
This can be very useful in cases where you need to create pickings in different warehouses from a single sale order.
